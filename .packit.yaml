# See the documentation for more information:
# https://packit.dev/docs/configuration/

specfile_path: cyan-skillfish-governor-smu-plus.spec

# name in upstream package repository or registry (e.g. in PyPI)
upstream_package_name: cyan-skillfish-governor-smu-plus
# downstream (Fedora) RPM package name
downstream_package_name: cyan-skillfish-governor-smu-plus


jobs:
- job: copr_build
  identifier: main
  trigger: commit
  branch: smu
  owner: filippor
  project: bazzite
  targets:
    - fedora-stable
    - fedora-development
    - fedora-rawhide

- job: copr_build
  identifier: release
  trigger: release
  owner: filippor
  project: bazzite
  targets:
    - fedora-stable
    - fedora-development
    - fedora-rawhide
